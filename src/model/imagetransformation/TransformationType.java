package model.imagetransformation;

/**
 * Enumerates the types of transformations that can be applied to image pixel matrices.
 */
enum TransformationType {
  FILTER, // Indicates a filtering operation
  TRANSFORM // Indicates a transformation operation.
}
